# Consul Properties
server.port=8080
spring.application.name: spring-cloud-register-service
management.security.enabled=false

#Couchbase Properties
spring.couchbase.bootstrap-hosts = localhost
spring.couchbase.bucket.name     = Administrator
spring.couchbase.bucket.password = Shekhar
spring.data.couchbase.auto-index = true
